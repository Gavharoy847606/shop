version: '3.8'

services:
  # Nginx Load Balancer (Trafikni taqsimlovchi)
  load_balancer:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8080:80"
    depends_on:
      - backend

  # ERP Backend (Bu erda ports qismini olib tashlaymiz, faqat LB orqali kiriladi)
  backend:
    build: ./frontend # (boyagi monorepo dockerfile joylashuvi)
    environment:
      - PORT=5000
      - DB_HOST=dpg-d808kc8sfn5c739appug-a.virginia-postgres.render.com
      - DB_NAME=localhost_mvo8
      - DB_USER=wed
      - DB_PASSWORD=QSeRb83uaJvzLMdkLrXlOEBALwq8W9Z4