version: '3.8'

services:
  # ERP Backend Xizmati
  backend:
    build: ./erp-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DB_USER=wed
      - DB_HOST=dpg-d808kc8sfn5c739appug-a.virginia-postgres.render.com
      - DB_NAME=localhost_mvo8
      - DB_PASSWORD=QSeRb03uaJvzLMdkLrXlOEBALwq8W9Z4
      - DB_PORT=5432

  # ERP Frontend Xizmati
  frontend:
    build:
      context: ./frontend
      args:
        - NEXT_PUBLIC_API_URL=http://localhost:5000
    ports:
      - "3000:3000"
    depends_on:
      - backend
